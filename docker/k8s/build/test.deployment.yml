apiVersion: apps/v1
kind: Deployment
metadata:
  name: monumenta-test-deployment
  namespace: build-ns
  labels:
    app: monumenta-test
spec:
  strategy:
    type: Recreate
  replicas: 0
  selector:
    matchLabels:
      app: monumenta-test
  template:
    metadata:
      labels:
        app: monumenta-test
    spec:
      volumes:
        - name: nfs-volume
          nfs:
            server: 192.168.1.90
            path: /home/bmarohn/SCRATCH
      containers:
      - name: monumenta-test
        image: localhost:32000/monumenta:latest
        tty: true
        stdin: true
        ports:
        - containerPort: 25565
        env:
        - name: "JAVA_MEM"
          value: "1024M"
        - name: "JAVA_JAR"
          value: "paperclip.jar"
        - name: "SERVER_DIR"
          value: "/home/rock/project_epic/test"
        volumeMounts:
        - name: nfs-volume
          mountPath: /home/rock
