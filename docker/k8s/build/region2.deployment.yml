apiVersion: apps/v1
kind: Deployment
metadata:
  name: monumenta-region2-deployment
  namespace: build-ns
  labels:
    app: monumenta-region2
spec:
  strategy:
    type: Recreate
  replicas: 0
  selector:
    matchLabels:
      app: monumenta-region2
  template:
    metadata:
      labels:
        app: monumenta-region2
    spec:
      volumes:
        - name: nfs-volume
          nfs:
            server: 192.168.1.90
            path: /home/bmarohn/SCRATCH
      containers:
      - name: monumenta-region2
        image: localhost:32000/monumenta:latest
        tty: true
        stdin: true
        ports:
        - containerPort: 25565
        env:
        - name: "JAVA_MEM"
          value: "4G"
        - name: "JAVA_JAR"
          value: "paperclip.jar"
        - name: "SERVER_DIR"
          value: "/home/rock/project_epic/region_2"
        volumeMounts:
        - name: nfs-volume
          mountPath: /home/rock
