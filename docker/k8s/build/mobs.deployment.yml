apiVersion: apps/v1
kind: Deployment
metadata:
  name: monumenta-mobs-deployment
  namespace: build-ns
  labels:
    app: monumenta-mobs
spec:
  strategy:
    type: Recreate
  replicas: 0
  selector:
    matchLabels:
      app: monumenta-mobs
  template:
    metadata:
      labels:
        app: monumenta-mobs
    spec:
      volumes:
        - name: nfs-volume
          nfs:
            server: 192.168.1.90
            path: /home/bmarohn/SCRATCH
      containers:
      - name: monumenta-mobs
        image: localhost:32000/monumenta:latest
        tty: true
        stdin: true
        ports:
        - containerPort: 25565
        env:
        - name: "JAVA_MEM"
          value: "768M"
        - name: "JAVA_JAR"
          value: "paperclip.jar"
        - name: "SERVER_DIR"
          value: "/home/rock/project_epic/mobs"
        volumeMounts:
        - name: nfs-volume
          mountPath: /home/rock
